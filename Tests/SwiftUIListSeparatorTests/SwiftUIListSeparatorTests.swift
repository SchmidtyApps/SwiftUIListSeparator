import XCTest
@testable import SwiftUIListSeparator

final class SwiftUIListSeparatorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIListSeparator().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
